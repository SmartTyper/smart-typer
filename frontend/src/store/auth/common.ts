enum AuthActionType {
  LOGIN = 'auth/login',
  REGISTER = 'auth/register',
  LOGOUT = 'auth/logout',
  LOGIN_GOOGLE = 'auth/loginGoogle',
  LOAD_USER = 'auth/loadUser',
}

export { AuthActionType };
