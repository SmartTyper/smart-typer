import { reducer as settings } from './reducer';
import { settings as settingsActions } from './actions';
import { SettingsActionType } from './common';

export { settings, settingsActions, SettingsActionType };
